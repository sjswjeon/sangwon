spring:
  profiles:
    active: real-db # ?? ?? ??

# local ??
---
spring:
  profiles: real-db
  datasource:
    url: jdbc:mariadb://database-1.cwmns3xrahu0.ap-northeast-2.rds.amazonaws.com:3306/webservice
    username: admin
    password: tkddnjs2
    driver-class-name: org.mariadb.jdbc.Driver
    initialization-mode: never
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true